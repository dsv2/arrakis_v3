INSERT INTO TRADES(ID,BOOK_ID, SECURITY_ID, CURRENCY, STATUS, QUANTITY, UNIT_PRICE, BUY_SELL, TRADE_DATE, SETTLEMENT_DATE)
select ID, BOOK.ID, SECURITY.ID, SECURITY.CURRENCY, SECURITY.STATUS
FROM SECURITY
JOIN BOOK ON SECURITY.ID = book.ID;
JOIN


CREATE TABLE SECURITYBOOK  AS(
SELECT BOOK.ID, SECURITY.COUPON, SECURITY.TYPE
FROM SECURITY
JOIN BOOK ON SECURITY.ID = book.ID);

select BOOK.ID,SECURITY.ID,COUNTERPARTY.ID,SECURITY.CURRENCY,SECURITY.STATUS,FULL_DATA.QUANTITY,FULL_DATA.UNIT_PRICE,FULL_DATA.TRADE_TYPE,FULL_DATA.TRADE_DATE,FULL_DATA.BOND_MATURITY_DATE
FROM SECURITY
JOIN BOOK ON SECURITY.ID = book.ID
JOIN COUNTERPARTY ON COUNTERPARTY.ID=SECURITY.ID
JOIN FULL_DATA ON FULL_DATA.TYPE=SECURITY.TYPE;